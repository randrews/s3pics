%script="loadJs('dialog');"

.header#randrews-lookingat-header
  -if @current_user
    =render :partial=>"logged_in_header"
  -else
    =render :partial=>"not_logged_in_header"

.body
  .image-selector
  .image-form=render :partial=>"image_form"
